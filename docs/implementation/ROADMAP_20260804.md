# 400 AI Finder 구현 로드맵 — 2026-08-04

- 상태: `active-plan`
- 총괄 EPIC: #1235
- 기준 `main`: `d0c20501def59015d8e419f2b675dff991b0e522`

이 로드맵은 기능 매력도가 아니라 **운영위험과 의존성** 순서로 정렬한다. 북구 golden baseline을 보호하며, 실제 시민 운영 전에 공개 API·근거정책·단일 원본을 먼저 완성한다.

## Phase 0 — 문서화와 범위동결

### 목표

현재 구현·미구현·검증·배포·승인 상태를 분리하고 개발 재개 전 기준선을 만든다.

### 산출물

- 현재 감사보고서
- 제품 트랙·운영경계
- 공개 API 보안·개인정보 모델
- unified SiteSpec 설계
- release gates
- contribution·security·GitHub templates
- 총괄 #1235와 P0·P1·P2 이슈

### 완료조건

- 문서 PR이 실행코드 변경 없이 CI를 통과한다.
- exact main·PR head·merge SHA가 기록된다.

## Phase 1 — 공개 endpoint 보호

### 관련

- #1224
- #1227

### 작업

1. request body byte·question length 계약
2. Turnstile 또는 동등 bot defense
3. IP·anonymous session·global rate limit
4. provider별 concurrency
5. 전체 request·provider timeout
6. request ID·latency·attempt telemetry
7. daily/monthly provider budget
8. kill switch·provider disable·snapshot-only fallback
9. abuse·timeout·budget staging smoke

### 완료조건

- 직접 HTTP와 browser path 모두 제한정책을 통과한다.
- 비용상한 초과 시 외부 provider가 중단된다.
- snapshot-only fallback이 시민 UI에서 정상작동한다.
- 질문원문·key·Authorization이 로그에 남지 않는다.

## Phase 2 — 근거 안전성

### 관련

- #1226
- #1080
- #1150

### 작업

1. evidence enum 고정
2. 고위험 claim taxonomy
3. field·claim별 최소 evidence level
4. model-only 확정표현 validator
5. source·claim 연결 metadata
6. stale snapshot 표시·refresh policy
7. 5 locale fail-closed 테스트
8. 잘못된 근거·기만 URL·outdated source 테스트

### 완료조건

- 근거 없는 연락처·기한·수수료·제출서류가 확정형으로 노출되지 않는다.
- canonical snapshot과 provider citation이 섞이지 않는다.
- unavailable 상태에서 공식 확인경로가 제공된다.

## Phase 3 — canonical SiteSpec

### 관련

- #1225

### 작업

1. current site IDs·names·domains inventory
2. SiteSpec schema
3. canonical ID·legacy alias
4. 기관명 effective date·locale display name
5. domain·entry point·crawl policy
6. clone golden metadata
7. Python profile adapter
8. Cloudflare metadata adapter
9. compatibility registry contract
10. dual-read migration

### 완료조건

- `bukgu`와 `bukgu_gwangju`가 명시적 alias 관계다.
- 기관명·domain·locale label이 단일 metadata에서 검증된다.
- golden route·public URL compatibility가 유지된다.

## Phase 4 — 공통 runtime 계약

### 관련

- #1228

### 작업

1. ProviderSpec
2. ActionSpec
3. EvidencePolicy schema
4. API schema
5. locale capability
6. Python·Cloudflare drift inventory
7. contract test
8. generated artifact 또는 verified adapter 결정

### 완료조건

- provider·action·site·freshness vocabulary drift가 CI에서 실패한다.
- runtime별 capability 차이는 명시적이다.
- secret env와 endpoint policy가 문서·schema로 관리된다.

## Phase 5 — CI 품질게이트

### 관련

- #1231

### 작업

1. Python contract job
2. snapshot·provenance job
3. Cloudflare Function job
4. citizen browser job
5. accessibility·responsive job
6. Page Agent job
7. comparison evidence job
8. build·packaging job
9. security·dependency job
10. dependency lock·cache
11. failure trace·screenshot artifact
12. required check·branch protection plan

### 완료조건

- 기존 모든 golden test가 job matrix에 포함된다.
- routine CI의 외부 network count가 0이다.
- 동일 commit의 dependency install이 재현 가능하다.
- 실패 job만 진단·재실행할 수 있다.

## Phase 6 — Cloudflare Function 모듈화

### 관련

- #1229

### 작업순서

1. responsibility inventory
2. response schema 추출
3. request validation 추출
4. provider config·runner 추출
5. official context·evidence policy 추출
6. locale policy 추출
7. telemetry 추출
8. handler façade 축소

### 원칙

- 한 PR에 좁은 extraction
- behavior change 금지
- public exports·API response compatibility 유지
- static/live build와 Function contract 확인

### 완료조건

- entrypoint가 orchestration 중심이다.
- provider·evidence·locale·telemetry가 독립 테스트 가능하다.

## Phase 7 — 시민 shell 모듈화

### 관련

- #1230

### 작업순서

1. state owner inventory
2. recommendation·composer 추출
3. locale 추출
4. browser history 추출
5. chat thread 추출
6. journey router 추출
7. accessibility·motion 추출
8. live bridge 추출
9. `window.CitizenFirstUseShell` façade 유지

### 완료조건

- golden DOM ID·data state·public API가 유지된다.
- desktop·mobile·locale·cancel·back/forward E2E가 통과한다.
- listener 중복과 late response 재진입이 없다.

## Phase 8 — 북구 fixture·visual coverage 지속

### 관련

- #1080

### 작업

- capture-required route의 provenance
- structure·content parity
- asset mapping
- interaction parity
- required viewport artifacts
- side-by-side visual review
- owner approval
- resident-default promotion

### 완료조건

각 route가 다음 상태를 독립적으로 가진다.

```text
captured
structurally_valid
asset_ready
interaction_valid
visually_approved
resident_default
```

## Phase 9 — 공식정보 freshness staging

### 관련

- #1150
- #1224
- #1227

### 작업

1. live source policy와 allowlist
2. exact requested/final URL
3. timeout·redirect·malformed URL fail-closed
4. real DOM selector validation
5. outage·stale·changed page behavior
6. retrieval timestamp·source update timestamp
7. rate limit·cache
8. protected staging UI integration

### 완료조건

- offline CI는 유지된다.
- live validation은 승인된 staging에서만 수행된다.
- snapshot과 live source의 precedence가 명확하다.
- production activation은 별도 gate다.

## Phase 10 — 다기관 supervised onboarding

### 관련

- #1181
- #1232

### 대상

1. 북구 generic projection
2. 다른 지자체
3. 교차도메인 사이트

### 산출물

- SiteSpec
- bounded capture report
- Site Model
- asset provenance
- clone preview
- knowledge index
- action graph
- confidence·exception queue
- QA report
- visual approval
- rollback

### 완료조건

- 새 기관마다 bespoke renderer를 만들지 않는다.
- 70~80% 자동화율과 human review 비율이 측정된다.
- 북구 golden resident-default는 parity 전 변경되지 않는다.

## Phase 11 — 저장소·라이선스 거버넌스

### 관련

- #1233
- #1234

### 작업

- remote branch read-only inventory
- merged·superseded·active·preserve 분류
- safe branch cleanup
- auto-delete merged branches 정책
- release tag·changelog·rollback 규칙
- code·fixture·asset·vendor license inventory
- owner-approved LICENSE·NOTICE 결정
- provenance checklist

### 완료조건

- 삭제증거 없는 branch는 보존된다.
- golden·release·rollback artifact는 보호된다.
- 공개자산의 license·source·version을 추적할 수 있다.

## Phase 12 — Authorized first-party integration

### 관련

- #862
- #873

### 시작조건

- 기관의 명시적 권한
- credentials·secret owner
- deployment ownership
- security review
- 개인정보 책임
- incident·support·rollback 책임

### 가능한 작업

- 기관 production widget
- authenticated search
- 실제 form integration
- 업무시스템 handoff

### 금지

위 시작조건 전에는 실제 submit·login·payment·PII·production write action을 구현·활성화하지 않는다.

## 첫 개발순서

```text
#1224
→ #1227
→ #1226
→ #1225
→ #1228
→ #1231
→ #1229
→ #1230
→ #1080
→ #1150
→ #1232
→ #1233 / #1234
→ #862 / #873
```

## 공통 PR 증거

- related issue
- exact base/head SHA
- changed files
- product track
- release gate
- network/provider mode
- data·PII·secret statement
- tests and browser evidence
- visual approval when applicable
- deployment impact
- rollback

## 중단조건

다음이 발생하면 범위를 늘리지 않고 중단·복구한다.

- golden route·DOM·state regression
- unauthorized external request
- actual submit·login·payment 발생
- secret·PII 노출
- official provenance 손실
- model-only answer의 공식사실 승격
- deployed SHA 불일치
- branch head 또는 review state 변경 후 미재검증
