# Local Visual-Clone Handoff

```text
Task:
Mode:
Issue:
Executor tier:
Branch:

Local HEAD before:
Remote HEAD before:
Local HEAD after:
Remote HEAD after push:

Approved reference IDs:
Approved source files:
Asset SHA-256 results:
Decoded dimensions:

Exact files changed:
Files intentionally untouched:
git diff --name-only:
git diff --check:
git status --porcelain=v1 --untracked-files=all:

Required commands and raw results:
1. Command:
   Exit code:
   Raw output:
2. Command:
   Exit code:
   Raw output:

Failure section:
Failed command:
Exit code:
Raw output or traceback:
Additional edits after failure:
Commit attempted:
Push attempted:

Local render viewport(s):
Render artifact path(s):
Comparison artifact path(s):
Browser console errors:
External request count:
localStorage writes:
sessionStorage writes:
Cookies:

Commit status:
Push status:
Known differences or blocked items:

No visual approval, CI approval, release approval, or merge approval is claimed
in this report.
```
