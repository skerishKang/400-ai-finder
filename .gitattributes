# Page Agent manifest-covered vendor artifacts are byte-stable.
# Their SHA-256 and byte count must match vendor-manifest.json on every OS,
# including Windows checkouts with core.autocrlf=true. Mark them -text so Git
# never applies line-ending conversion on checkout.
src/web/examples/page-agent/vendor/LICENSE -text
src/web/examples/page-agent/vendor/page-agent.iife.js -text

# #1160/#1166 official home capture + generated clone fixture are byte-stable
# checksum identity sources. Keep LF bytes on every OS checkout.
data/official_captures/bukgu_gwangju/home/** -text
data/official_captures/seogu_gwangju/** -text
data/official_clone_fixtures/** -text

# #1303 Seo-gu G1 capture plan is the approved plan identity source. Its
# committed SHA-256 is recorded in the executed ledger, so keep LF bytes
# byte-stable on every OS checkout (including core.autocrlf=true).
configs/reference-plans/seogu_gwangju.json -text
# #1356 S5 Seo-gu passport guidance capture plan is an approved plan
# identity source (its committed SHA-256 is recorded in the executed
# ledger). Keep LF bytes byte-stable on every OS checkout (including
# core.autocrlf=true) so the ledger plan-checksum gate holds locally.
configs/reference-plans/seogu_gwangju_passport_guidance.json -text
# #1343 S3 housing + S2/S7/S8 handoff capture plans are approved plan
# identity sources (their committed SHA-256 is recorded in the executed
# ledgers). Keep LF bytes byte-stable on every OS checkout (including
# core.autocrlf=true) so the ledger plan-checksum gates hold locally.
configs/reference-plans/seogu_gwangju_housing.json -text
configs/reference-plans/seogu_gwangju_handoff_evidence.json -text
# #1360 S6 Seo-gu unmanned-kiosk capture plan is an approved plan identity
# source (its committed SHA-256 is recorded in the executed ledger). Keep LF
# bytes byte-stable on every OS checkout (including core.autocrlf=true) so the
# ledger plan-checksum gate holds locally.
configs/reference-plans/seogu_gwangju_unmanned_kiosk.json -text

# #1177 home asset identity audit report + frozen scan manifest must keep
# committed LF JSON bytes on every OS (including core.autocrlf=true).
data/official_clone_asset_audits/bukgu_gwangju/home-repository-match-audit.json -text
data/official_clone_asset_audits/bukgu_gwangju/home-repository-scan-manifest.json -text
