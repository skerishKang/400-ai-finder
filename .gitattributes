# Page Agent manifest-covered vendor artifacts are byte-stable.
# Their SHA-256 and byte count must match vendor-manifest.json on every OS,
# including Windows checkouts with core.autocrlf=true. Mark them -text so Git
# never applies line-ending conversion on checkout.
src/web/examples/page-agent/vendor/LICENSE -text
src/web/examples/page-agent/vendor/page-agent.iife.js -text

# #1160/#1166 official home capture + generated clone fixture are byte-stable
# checksum identity sources. Keep LF bytes on every OS checkout.
data/official_captures/bukgu_gwangju/home/** -text
data/official_clone_fixtures/** -text

# #1177 home asset identity audit report + frozen scan manifest must keep
# committed LF JSON bytes on every OS (including core.autocrlf=true).
data/official_clone_asset_audits/bukgu_gwangju/home-repository-match-audit.json -text
data/official_clone_asset_audits/bukgu_gwangju/home-repository-scan-manifest.json -text
